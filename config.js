// we can user this config.js to change server port and host details
// it is like environment file
module.exports = {
    port: 3000, // port
    Db_con :{   // database connection details
        host: "localhost",
        user: "root",
        password: ""
      }
  }